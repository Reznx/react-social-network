import React from "react";
import { NavLink } from 'react-router-dom';

const Login = () => {
    return (
      <h2>
        <NavLink to={"/login"}>Login</NavLink> PLEASE
      </h2>
    );
}

export default Login;